{% include "base.html"%}
{% set screen_data = screen_data %}
<script >
      var form = {
        view:"form",
        id:"form",
        borderless:true,
        elements:[
            {cols:[
                {},
                {view:"label", label:"GetTest"},
                {}
            ]}
    ]};
    var logic = {
        init:function() {
        },
        pageMove:function(page) {
        }
    };
    webix.ready(function () {
        webix.ui(
            {rows:[
                form,
            ]}
        );
        logic.init();
    });
</script>