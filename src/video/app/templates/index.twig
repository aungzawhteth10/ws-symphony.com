{% include "base.html.twig"%}
<style>
  .pic {
    width: 300px;
    height: 200px;
  }
  .title{
    text-decoration: none;
  }
  span.time {
    background-color: grey !important;
    border-radius: 20% !important;
  }
  footer {
    text-align: center;
    font-size: 10px;
    color: white !important;
  }
</style>
<script>
  var form = {
    view:"form",
    id:"form",
    borderless:true,
    elements:[
      {rows:[
        {template:"{{video_template|raw}}", borderless:true, scroll:true},
      ]}

  ]};
  var logic = {
    init:function() {
    },
    pageMove:function(page) {
      WSS.pageMove(page);
    }
  };
  webix.ready(function () {
    webix.ui(
      {rows:[
        form,
        WSS.footer
      ]}
    );
    logic.init();
  });
</script>
